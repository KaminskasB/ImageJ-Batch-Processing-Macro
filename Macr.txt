function removeBackground(input, output, filename) {
	open(input + filename);
	run("Brightness/Contrast...");
	run("Enhance Contrast", "saturated=0.35");
	run("Apply LUT");
	run("Close");
	run("Unsharp Mask...", "radius=1 mask=0.60");
	saveAs("Tiff", output + filename);
	close();
}

input = "/home/benediktaskaminskas/Desktop/Original pictures used in figures/Figure 1/B options/"
output = "/home/benediktaskaminskas/Desktop/Original pictures used in figures/Figure 1/B options/MOD"

setBatchMode(true); 
imageList = getFileList(input);
for (i = 0; i<imageList.length; i++)
	removeBackground(input, output, imageList[i]);
setBatchMode(false);
